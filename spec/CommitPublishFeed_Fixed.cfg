\* The fix: atomic, journal-ordered publish. TLC must find NO invariant violation.
SPECIFICATION Spec
CONSTANTS
    Writers = {w0, w1, w2}
    AtomicPublish = TRUE
\* Terminal states (every writer done) are intended end states, not deadlocks.
CHECK_DEADLOCK FALSE
INVARIANT TypeOK
INVARIANT FeedMatchesStore
